#!/usr/bin/env monkey

//
// This program dumps the command-line arguments the script received.
//

let a = args();

puts( "We received ", len(a), " arguments to our script.\n" );

let i = 0;
for( i < len(a) ) {
   puts( "\t", i, " " , a[i], "\n");
   i++
}
