#!/usr/bin/env monkey

//
// This program demonstrates the use of variable-updates without the need
// for `let`.
//


let i = 3;
i = 2 * PI ;
puts( "2xPI ", i, "\n" );

x = 3;
puts("X:", x, "\n" );

//
// The following works, but it is a fatal-error to assign a value
// to an unknown variable when running with `pragma("strict");`.
//
yfds = 33.312;
puts("X:", yfds, "\n" );
